using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using UnityEngine;

/*
    "Mirrorable": {
      "MirrorBlockName": "GSOAIController_111", // the EXACT name of the block to mirror (or the BlockType if Vanilla)
      // if the block already has a pair than this will fail.
    },
 */
public class Mirrorable : MonoBehaviour
{
    public string MirrorBlockName;
}
